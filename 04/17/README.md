### Exercise 4.17

Explain the difference between prefix and postfix increment.

### Solution

The prefix increment operator increments its operand and returns that value,
while the postfix increment operator returns its unmodified value to be
incremented definitely after execution of the complete statement.
